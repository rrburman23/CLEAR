def filter_even_numbers(numbers: list) -> list:
    """Returns a list of only the even numbers from the input."""
    evens = []
    for num in numbers
        # INTENTIONAL BUG: Missing colon on the line above, and missing bracket below.
        if num % 2 == 0:
            evens.append(num
    retun evens # INTENTIONAL BUG: Typo in the return statement